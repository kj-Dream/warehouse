<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.warehouse.mapper.ProductCategoryMapper">
    <resultMap id="BaseResultMap" type="com.example.warehouse.pojo.ProductCategory">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="category_name" property="categoryName"/>
        <result column="category_code" property="categoryCode"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">SELECT * FROM product_category ORDER BY id</select>
    <select id="findById" resultMap="BaseResultMap">SELECT * FROM product_category WHERE id=#{id}</select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_category (parent_id, category_name, category_code) VALUES (#{parentId}, #{categoryName}, #{categoryCode})
    </insert>

    <update id="updateById">
        UPDATE product_category
        <set>
            <if test="categoryName != null">category_name=#{categoryName},</if>
            <if test="categoryCode != null">category_code=#{categoryCode},</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="deleteById">DELETE FROM product_category WHERE id=#{id}</delete>
    <select id="countChildren" resultType="int">SELECT COUNT(*) FROM product_category WHERE parent_id=#{parentId}</select>
    <select id="countProducts" resultType="int">SELECT COUNT(*) FROM product WHERE category_id=#{categoryId}</select>
</mapper>
