<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.warehouse.mapper.ProductMapper">
    <resultMap id="BaseResultMap" type="com.example.warehouse.pojo.Product">
        <id column="id" property="id"/>
        <result column="product_code" property="productCode"/>
        <result column="product_name" property="productName"/>
        <result column="category_id" property="categoryId"/>
        <result column="unit" property="unit"/>
        <result column="cost_price" property="costPrice"/>
        <result column="sale_price" property="salePrice"/>
        <result column="min_stock" property="minStock"/>
        <result column="max_stock" property="maxStock"/>
        <result column="image_url" property="imageUrl"/>
        <result column="status" property="status"/>
    </resultMap>

    <sql id="whereCondition">
        <where>
            <if test="productName != null and productName != ''">
                AND (product_name LIKE CONCAT('%',#{productName},'%') OR product_code LIKE CONCAT('%',#{productName},'%'))
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </sql>

    <select id="findById" resultMap="BaseResultMap">SELECT * FROM product WHERE id = #{id}</select>

    <select id="search" resultMap="BaseResultMap">
        SELECT * FROM product WHERE status = 1 AND (product_name LIKE CONCAT('%',#{keyword},'%') OR product_code LIKE CONCAT('%',#{keyword},'%')) LIMIT 50
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT * FROM product <include refid="whereCondition"/> ORDER BY id LIMIT #{offset}, #{limit}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM product <include refid="whereCondition"/>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (product_code, product_name, category_id, unit, cost_price, sale_price, min_stock, max_stock, status)
        VALUES (#{productCode}, #{productName}, #{categoryId}, #{unit}, #{costPrice}, #{salePrice}, #{minStock}, #{maxStock}, COALESCE(#{status},1))
    </insert>

    <update id="updateById">
        UPDATE product
        <set>
            <if test="productCode != null">product_code=#{productCode},</if>
            <if test="productName != null">product_name=#{productName},</if>
            <if test="categoryId != null">category_id=#{categoryId},</if>
            <if test="unit != null">unit=#{unit},</if>
            <if test="costPrice != null">cost_price=#{costPrice},</if>
            <if test="salePrice != null">sale_price=#{salePrice},</if>
            <if test="minStock != null">min_stock=#{minStock},</if>
            <if test="maxStock != null">max_stock=#{maxStock},</if>
            <if test="status != null">status=#{status},</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="deleteById">DELETE FROM product WHERE id=#{id}</delete>
</mapper>
